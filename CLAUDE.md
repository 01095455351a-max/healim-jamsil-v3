# 해아림한의원 잠실점 홈페이지

Hugo(extended)로 만든 정적 사이트. 테마를 쓰지 않고 레이아웃을 직접 작성했다.

## 실행

```
run-hugo-server.bat          # Windows. http://127.0.0.1:1315
hugo server --disableFastRender --bind 127.0.0.1 --port 1315
```

Hugo **0.146.0 이상**이 필요하다. `layouts/_partials/`, `layouts/_default/` 구조가
0.146에서 도입된 배치를 따른다. `.bat`은 실패해도 창이 닫히지 않고 원인을 표시한다.

서버를 띄운 채 `git pull`하면 파일 변경이 감지되어 브라우저가 자동으로 새로고침된다.

## 구조

```
content/         질환 9 · 후기 17 · 컬럼 12 · 원장 2 · 공지 2
                 첫 내원 · 개인정보처리방침 · 이용약관 · 비급여안내
data/
  clinic.yaml            연락처·진료시간·주소·좌표·예약 링크. 거의 모든 레이아웃이 참조
  treatments.yaml        치료 방법 9종. 홈의 팝업 내용
  pricing.yaml           비급여 항목·금액. 줄을 늘리면 화면에 그대로 늘어난다
  authors/<slug>.yaml    원장 정보. 프로필·진료분야·연구 이력·논문
layouts/
  index.html             홈 (11개 구역)
  legal.html             개인정보처리방침·이용약관 공용
  pricing.html           비급여안내
  _partials/             header · footer · kakao-map · image · paper-row 등
  <섹션>/single|list.html
assets/
  css/site.css           전역 CSS. 인라인 style로 덮이지 않는 부분만 담당
  design-v3/             질환·도서·로고 원본
  media/authors, media/clinic   원장 사진, 원내 사진
  media/reviews                 자필 후기 스캔 30장(A4 300dpi). 440px WebP로만 내보낸다
static/js/               hero-slides · treatment-modal · filters · review-slider
```

## 이 프로젝트의 규칙

**파일을 고치기 전에 먼저 알리고 확인을 받는다.** 무엇을·왜·어디를 고칠지와
나중에 관리할 때 어떤 영향이 있는지를 함께 알린 뒤, 승낙을 받고 실행한다.
진단하고 바로 고쳐서 보고하는 방식은 쓰지 않는다.
읽기·빌드·화면 확인처럼 파일을 바꾸지 않는 작업은 확인 없이 해도 된다.

**작업은 나중에 고치기 쉬워야 한다.** 새 기능을 만들 때 "이걸 나중에 누가 어떻게
고치는가"를 함께 판단한다. 두 가지를 지킨다 — 같은 값을 화면 여러 곳에 흩어 놓지
말고 `data/`의 파일 한 곳에 둔다. 파일을 추가할 때마다 레이아웃을 고쳐야 하는
구조는 만들지 않는다(자필 후기·원내 사진처럼 폴더에 넣으면 붙게 한다).

**디자인은 Claude Design mockup(`handoff/design-v3/`)이 원본이다.**
색상·폰트·여백·카드 스타일을 임의로 바꾸지 않는다. 새 요구가 들어오면
기존 컴포넌트를 재사용하는 쪽을 먼저 찾는다. (mockup 원본 파일은 저장소에 없다.
로컬에 있으면 커밋해 두는 것이 좋다.)

**스타일은 인라인 style이 기본이다.** mockup이 인라인 기반이라 그대로 옮겼다.
site.css는 hover·반응형·컴포넌트 동작처럼 인라인으로 표현할 수 없는 것만 담는다.
인라인 style을 CSS에서 덮어야 할 때는 `!important`가 필요하다 — 기존 코드가 이미
그 방식을 쓴다.

**JS는 최소한으로.** 가능하면 체크박스+CSS로 해결한다(헤더 햄버거, 논문 더보기).
스크립트가 필요하면 `window.init...`을 노출해 다시 호출할 수 있게 한다.

**의료광고 표현.** 논문·후기를 근거로 치료 효과를 단정하지 않는다.
"참여했습니다 / 보고했습니다 / 연구했습니다"처럼 사실만 적는다.

**원장 컬럼·공지사항을 쓸 때는 `docs/컬럼-작성-가이드.md`를 먼저 읽는다.**
기존 12편에서 뽑아낸 문체·뼈대·front matter 규칙과, 참고자료 주소를 지어내지
않기 위한 절차가 적혀 있다.

**사실이 아닌 내용을 채워 넣지 않는다.** 실제 정보가 없으면 비워 두고 표시하지
않는다. 자리표시자를 남길 때는 화면에서 구분되게 한다. 홈의 학술 논문이 한때
가짜 제목으로 채워져 있었다 — 같은 일을 반복하지 않는다.

## 지금까지의 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| 치료 방법 상세 | 개별 페이지 없이 홈의 팝업 | 이미 팝업으로 제작됨 |
| 치료법 링크 | `/#treatment-<slug>` 딥링크 | 팝업을 유지하면서 외부에서 링크 가능 |
| 팝업 폭(데스크톱) | `max-width: 708px` | box-sizing 수정 전 렌더 폭 유지 |
| 팝업(모바일) | 하단 시트 + 제목·닫기 고정 | 폭이 화면을 넘어 닫기 버튼이 사라지던 문제 |
| 학술 논문 | 홈에만 표시, 원장 데이터에서 취합 | 두 곳에 두면 어긋남 |
| 논문 순서 | 석선희 원장 제1저자 먼저 | 파일에 적힌 순서대로 표시됨 |
| 논문 링크 | 원문 링크를 걸지 않는다 | 제목·학회지·서지사항만 표시 |
| 원내 사진 | 히어로 한 자리에서 6장 슬라이드, 둘러보기 구역 폐지 | 사진이 크게 보이고 구역이 하나 줄어듦 |
| 이미지 | 원본은 assets/, 화면 크기에 맞춰 WebP 변환 | 55MB → 0.6MB |
| 전화번호 | 안심번호 0507-1469-7578로 표시·연결 통일 | `data/clinic.yaml` 두 줄에만 있다 |
| 이용약관 | 구 사이트에 내용이 없어 새로 작성(13개 조항) | 시행일 2026-09-01 |
| 도메인 | `https://healim-jamsil.com/` | |
| 자필 후기 | 로그인 없이 스캔 30장을 가로 슬라이드로 공개 | 정적 사이트로 로그인 불가, 검색 유입도 막힘 |
| 스캔 원본 | `assets/`에 두고 440px WebP만 내보냄 | 300dpi 원본이 URL로 노출되지 않는다 |
| 후기 더보기 | 네트워크 홈페이지(`/5-3`)로 링크 | 잠실점에서 전부 관리하지 않는다 |
| 상담 신청 폼 | 폼을 걷어내고 전화·카카오·네이버 버튼으로 | 정적 사이트라 받을 서버가 없다. 수집하지 않으면 개인정보 의무도 없다 |
| 카카오·네이버 버튼 | 각 서비스의 브랜드 색을 그대로 | 어느 통로인지 한눈에 보인다 |
| 햄버거 전환 폭 | 949px 이하 | 940px 아래에서 상단 메뉴가 두 줄로 접힌다. 아래 고정 막대·히어로 버튼·푸터 버튼이 같은 폭을 쓴다 |
| 글자·여백 축소 폭 | 767px 이하 | 800px 태블릿은 화면이 넓어 데스크톱 글자 크기가 알맞다 |
| 치료 방법 묶음 | 주 치료 3 + 나머지 6, 색으로만 구분 | 묶음 이름을 두 개 두면 침치료가 홀로 남았다 |
| 검색 제목 | 지역·증상 검색어를 페이지마다 나눠 담는다 | 한 제목에 나열하면 잘리고 과최적화로 읽힌다 |
| 원장 사진 | 동그란 자리는 `card-<원장>.jpg` 사본을 쓴다 | 3:4 원본을 가운데로 자르면 머리가 잘린다 |

## 이미지 다루기

`static/`이 아니라 **`assets/`**에 둔다. 마크업에서는 partial을 쓴다.

```
{{ partial "image.html" (dict "src" "design-v3/disease-adhd.png" "w" 760
    "alt" "ADHD" "style" "width: 100%; height: 100%; object-fit: cover;") }}
```

`w`는 화면 표시 폭의 2배를 준다. 첫 화면에 보이는 이미지는 `"eager" true`.
원본을 압축하지 말고 그대로 넣으면 된다.

## 검토 모드

`hugo.yaml`의 `params.noindex`가 스위치다. `true`이면 모든 페이지에
`noindex, nofollow` 메타가 붙고 `robots.txt`가 전체 수집을 막는다. `false`이면
메타가 사라지고 robots.txt가 sitemap을 안내한다.

**지금은 `false`다**(2026-08-30 공개). 크게 뜯어고치는 동안 잠시 감추고 싶으면
`true`로 되돌리면 되지만, 이미 검색엔진에 등록한 뒤라 오래 두지 않는다.

## 배포

**2026-08-30 정식 공개했다.** `https://healim-jamsil.com`

| 항목 | 내용 |
|---|---|
| 호스팅 | Cloudflare Pages (프로젝트 `healim-jamsil`) — master에 푸시하면 1~2분 뒤 반영 |
| 빌드 명령 | `hugo --gc` — **`--minify`를 붙이지 않는다**(아래) |
| 산출물 | `public` |
| 빌드 변수 | `HUGO_VERSION = 0.146.0` |
| 도메인 | Cloudflare 등록. DNS는 CNAME 두 줄(`@`, `www`) 모두 `healim-jamsil.pages.dev`로 Proxied |
| www | Redirect Rule로 `healim-jamsil.com`에 301로 넘긴다 |
| 검색 수집 | 열려 있다(`params.noindex: false`) |
| 네이버 | 서치어드바이저 소유 확인·사이트맵 제출 완료. 확인 코드는 `hugo.yaml`의 `params.naver_verification` |
| 구글 | 서치 콘솔 도메인 속성. Cloudflare DNS 자동 연동으로 확인, 사이트맵 32개 인식 |

`--minify`를 쓰면 안 된다 — 인라인 style의 공백까지 지워서 모바일 정돈 블록의
`[style*="font-size: 34px"]` 선택자가 하나도 걸리지 않는다. 화면이 깨지지는 않고
모바일에서 글자와 여백만 데스크톱 크기로 나와 알아채기 어렵다. 실제로 붙여서
빌드하면 390px에서 홈이 가로로 274px 넘친다. 같은 내용이 `wrangler.jsonc` 맨 위에도 있다.

건드리면 안 되는 것 — `hugo.yaml`의 `naver_verification`과 Cloudflare DNS의
구글 TXT 레코드. 지우면 소유 확인이 풀린다.

## 검색 제목(title)

브라우저 탭이자 검색 결과의 파란 제목이다. **한글 30자 안팎에서 잘린다.**

- 홈은 `hugo.yaml`의 `params.title_home` 한 줄이 통째로 쓰인다
- 나머지는 `title_seo`가 있으면 그것을, 없으면 `title`을 쓰고 뒤에
  `| 해아림한의원 잠실점`이 자동으로 붙는다

지역·증상 검색어를 페이지마다 나눠 담았다(홈에 몰아넣지 않는다). 질환 9개는
전부 `title_seo`를 갖고 있다. 검색엔진에 등록한 뒤이므로 **자주 바꾸지 않는다** —
바뀔 때마다 순위가 자리 잡는 데 시간이 더 걸린다.

## 원장 사진

동그랗게 잘라 쓰는 자리는 `_partials/author-photo.html`을 쓴다(홈 의료진 96px,
홈·목록 컬럼 카드 32px, 컬럼 본문 44·56px, 질환 페이지 48px).

`assets/media/authors/card-<원장>.jpg`가 있으면 그것을 쓴다 — 동그란 칸에 맞춰
미리 정사각으로 잘라 둔 사본이라 원장 사이에 얼굴 크기가 어긋나지 않는다. 없으면
원본을 위 기준으로 자른다(3:4 원본을 가운데로 자르면 머리 윗부분이 날아간다).

원장 상세페이지의 큰 사진은 이 partial을 쓰지 않고 늘 원본을 쓴다.

## 글 관리 화면 (Decap CMS)

`/admin/`에서 원장 컬럼과 공지사항을 브라우저로 쓰고 고친다. 저장하면 저장소에
커밋되고 Cloudflare Pages가 다시 빌드한다 — **지금과 같은 정적 사이트 그대로다.**
검색엔진과 AI 크롤러는 빌드가 끝난 완전한 HTML을 받는다.

| 파일 | 하는 일 |
|---|---|
| `static/admin/config.yml` | 컬렉션·필드 정의. 규칙이 전부 여기 있다 |
| `static/admin/index.html` | 관리 화면. Decap을 CDN에서 받는다(판올림은 이 줄의 번호만) |
| `static/_headers` | `/admin/*`에 noindex. `layouts/robots.txt`를 건드리지 않으려고 여기서 한다 |
| `docs/CMS-사용법.md` | 원장용 안내 |

**세 가지를 어기면 검색 노출이 조용히 깨진다.**

1. **본문은 `widget: text`다.** 위지윅을 쓰면 본문 속 내부 링크 30여 곳이 저장할
   때 변환되며 깨질 수 있다. `markdown` 위젯에 `modes: ['raw']`를 주는 방법을
   먼저 시험했으나 Decap 3.15가 그 위젯을 richtext로 갈아치우면서 옵션이 먹지
   않았다(도구막대가 그대로 떴다). `text`는 판올림에 영향받지 않는다.
2. **Decap은 설정에 없는 front matter 항목을 저장할 때 지운다.** `content/`에 새
   항목을 더하면 `config.yml`에도 같은 이름으로 반드시 넣는다. 지금은 실제 파일에
   있는 항목을 모두 적어 두었다(`_build`, `description`까지).
3. **필드 이름은 `_partials/schema.html`이 읽는 이름과 같아야 한다.** 어긋나면
   FAQPage·Article이 화면은 멀쩡한 채로 사라진다.

슬러그는 자동 생성하지 않는다. 새 글만 「주소에 쓸 영문 이름」에 적고, 있던 글은
비워 둔다 — 빈 값이 들어가도 Hugo가 파일명을 그대로 쓴다(시험으로 확인).

**로그인.** Cloudflare Pages에는 Netlify 같은 기능이 없어 GitHub OAuth를 직접
중개한다 — `functions/api/auth.js`가 GitHub로 넘기고 `callback.js`가 받은 코드를
열쇠로 바꿔 CMS에 건넨다. Client ID·Secret은 Cloudflare Pages의 환경 변수
(`GITHUB_OAUTH_ID`·`GITHUB_OAUTH_SECRET`)에만 있고 저장소에는 없다.
OAuth App의 callback 주소는 `https://healim-jamsil.com/api/callback` 이며,
이 주소가 바뀌면 GitHub 쪽 설정도 함께 고쳐야 한다.
내 컴퓨터에서 쓸 때는 `npx decap-server`를 함께 띄운다(`local_backend: true`).

## 아직 남은 일

- **대표원장 친필 서명.** 자리는 만들어 두었다 —
  `data/authors/seok-seonhui.yaml`의 `signature`에 assets 기준 경로를 적으면
  홈 소개 글 아래에 나온다. 배경이 투명한 PNG를 쓴다.
- **공지사항을 공개할지.** `content/notice/website-open.md`가 `render: never`다.
- **비급여안내를 공개할지.** `content/pricing.md`가 `render: never`다.
  의료법상 비급여 진료비용 고지 의무가 있어 공개를 권한다. 내용은 이미
  `data/pricing.yaml`에 있고 푸터 링크만 되살리면 된다.
- **산후우울증 컬럼 초안**이 `docs/초안/`에 있다. 한의학적 치료 접근과 상담 내용을
  보충하고 원장 검수를 받은 뒤 `content/column/`으로 옮긴다.
- **류석균 원장 출연 영상 제목**을 모른다. 지금은 「류석균 원장 출연 영상」으로 나온다.
- **후기 이미지 없음.** `content/reviews/`의 front matter `image:` 경로에 해당하는
  파일이 없다. 이 경로는 자필 후기 스캔(`assets/media/reviews/`)과 별개다.

## 폐기한 것

- **상담 신청 폼.** 정적 사이트라 제출을 받을 서버가 없고, 폼 서비스를 붙이면
  건강 정보가 국외로 나가 개인정보 보호법 제28조의8(국외 이전) 문제가 생긴다.
  대신 전화·카카오톡 채널·네이버 예약 버튼 세 개를 둔다. 셋 다 `data/clinic.yaml`을 본다.

  나중에 폼이 필요해지면 커밋 `36f8e14`에 마지막 폼 마크업이 남아 있다.
  받는 쪽은 Cloudflare Pages Functions로 만들어 국내 메일로 전달하는 방식을 권한다
  (해외 폼 서비스는 국외 이전 문제로 권하지 않는다).
- **네이버·카카오 로그인.** 서버가 필요하고, 개인정보 수집 의무가 새로 생기고,
  로그인 뒤 내용은 검색에 잡히지 않는다. 자필 후기는 로그인 없이 공개한다.

## 모바일

모바일(767px 이하)에서 글자와 여백을 줄이는 규칙이 `site.css` 끝의 **모바일 정돈**
블록에 모여 있다. mockup이 데스크톱 기준이라 그리드 스택과 햄버거 말고는 반응형이
없었던 것을 나중에 채운 것이다. HTML은 건드리지 않았다.

인라인 style을 값으로 찾아서 덮는 방식이라 편한 대신 약점이 하나 있다.

```
[style*="font-size: 34px"] { font-size: 25px !important; }
```

**레이아웃에 새로운 px 값을 쓰면 모바일 규칙이 조용히 안 걸린다.** 34px을 33px로
바꿔도 마찬가지다. 데스크톱만 보고 있으면 알아채기 어렵다.

그래서 점검 스크립트를 두었다. 레이아웃을 고친 뒤 실행한다.

```
python3 tools/check-mobile.py
```

빠진 값을 목록으로 알려 준다. 나오면 `site.css`의 모바일 정돈
블록에 한 줄 추가하면 된다. 기존 규칙을 그대로 흉내 내면 된다.

새 구역을 만들 때 **기존에 쓰던 px 값을 그대로 쓰는 것이 가장 편하다.** 그러면
규칙을 추가할 일이 없다.

## 자필 후기 추가·교체

`assets/media/reviews/`에 파일을 넣으면 슬라이드에 자동으로 붙는다. 파일명 순서대로
표시되므로 번호를 매긴다(`32.jpg`). 지우면 사라진다. 레이아웃은 손댈 필요가 없다.

원본은 A4 300dpi 그대로 넣는다. 화면에는 440px WebP로만 나가고 **원본은 사이트에
올라가지 않는다** — 주소를 알아도 300dpi 스캔을 받을 수 없다.

넣기 전에 성명·연락처가 보이는지 확인한다. 현재 30장은 양식 자체가 `최**`, `박○○`
형태라 별도 처리가 필요 없었다.

## 확인 방법

변경 후에는 이 네 가지를 본다.

1. 빌드가 통과하는가 — `hugo --gc` (**`--minify`를 붙이지 않는다** — 배포 항목 참고)
2. 모바일 규칙이 빠지지 않았는가 — `python3 tools/check-mobile.py`
3. 내부 링크가 살아 있는가 — 산출물의 `href="/..."`를 파일 존재 여부와 대조
4. 모바일에서 넘치지 않는가 — 브라우저를 390px 폭으로 줄여 가로 스크롤 확인
